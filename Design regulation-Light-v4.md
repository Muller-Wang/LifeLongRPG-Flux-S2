# Questopia 视觉设计规范 v3.0 · 机能晴空

> **设计方向**：以看板娘「绯夏」的机能风造型为核心锚点，融合《绝区零》的都市机能美学与《明日方舟》的工业功能主义，构建一套**以浅蓝晴空为基底、几何感强烈、干练利落**的人生管理 RPG 视觉体系。整体追求"强对比下的自然柔和"——直角与粗线传递机能感，弥散渐变与留白保持呼吸感。
>
> **背景资产**：
> - 启动界面（Splash）：`/Users/stevenliu/Desktop/绯夏主页v2.png` —— 全屏角色展示，登录注册以弹窗呼出
> - 全局背景（All Screens）：`/Users/stevenliu/Desktop/绯夏主界面.png` —— 主界面及所有拓展界面的统一背景

---

## 一、设计哲学

### 1.1 核心关键词

**机能晴空 · 几何秩序 · 冷暖对撞**

绯夏身着黑色机能风服装、银白双麻花辫、点缀蓝色小花与工牌，站在浅蓝晴空背景中，形成鲜明的**深色机能剪影跃入晴空**的画面。视觉体系围绕这一锚点展开：
- **晴空蓝白渐变背景** → 全局背景图 `绯夏主界面.png`，开阔、柔和、有呼吸感
- **深蓝/靛蓝** → 核心文本、标题、功能边界，建立秩序
- **向日葵金** → 仅用于核心指引、进度、重要状态，如启动界面顶部的黄色装饰带
- **角色右侧布局** → 主界面绯夏固定于右侧，左侧留白给功能与文本
- **直角粗线** → 所有容器、输入框、按钮的边界语言
- **无背景指引** → 导航与操作指引拒绝色块包裹，以图标+加粗文字直接呈现

### 1.2 视觉锚点

| 锚点 | 来源 | 在产品中的体现 |
|------|------|---------------|
| 晴空渐变 | `绯夏主界面.png` | 所有功能页面的全屏背景，统一视觉基底 |
| 全屏启动 | `绯夏主页v2.png` | 启动界面完整全屏展示，功能以弹窗Overlay呼出 |
| 耶鲁蓝 | 配色方案 #084B83 | 主按钮、粗线边框、核心标题 |
| 向日葵金 | 配色方案 #FFBC42 | 进度条、当前状态指示、启动界面顶部装饰带 |
| 太空靛蓝 | 配色方案 #192A51 | Display 大标题、核心数据，压阵感 |
| 直角粗线 | 绝区零 + 机能风 | 所有组件 0px 圆角，2px 实线边框 |
| 无背景指引 | 主界面截图 | 导航与操作区纯文字+图标，拒绝卡片包裹 |
| 几何装饰 | 背景图自带 | 点阵、细线网格、半透明圆弧作为全局背景纹理 |

---

## 二、色彩系统

### 2.1 主色板（Primitive Colors）

```
Alice Blue        #DCEDFF  ■  页面最底层背景 · 晴空色
Powder Blue       #94B0DA  ■  次级背景、悬停态、装饰性几何纹理
Yale Blue         #084B83  ■  主功能色 · 按钮/选中态/粗线边框/核心标题
Space Indigo      #192A51  ■  Display 文字 · 大标题/核心数据 · 压阵深蓝
Sunflower Gold    #FFBC42  ■  高亮色 · 进度/当前状态/极少数强调 · 如警示带般精准
White             #FFFFFF  ■  内容卡片背景、文本框底色
Slate Grey        #5A6A7E  ■  次要文字、描述、辅助图标
Mist Grey         #E2E8F0  ■  分隔线、禁用状态
```

### 2.2 语义色板（Semantic Colors）

| 语义 | 色值 | 使用场景 |
|------|------|---------|
| **Background** | `#DCEDFF` | 页面全局底层背景（背景图之上的兜底色） |
| **Surface** | `#FFFFFF` | 内容卡片、文本框、弹窗底色 |
| **Surface Hover** | `#F0F7FF` | 卡片/列表项悬停态 |
| **Surface Elevated** | `#084B83` | 主按钮背景、选中态填充 |
| **Surface Overlay** | `rgba(25, 42, 81, 0.35)` | 弹窗遮罩、模态层（带 backdrop-blur: 16px） |
| **Primary** | `#084B83` | 主按钮、粗线边框、核心标题、选中指示器 |
| **Primary Hover** | `#0A5CA0` | 按钮悬停、链接悬停（比 Yale Blue 稍亮 8%） |
| **Accent** | `#FFBC42` | 当前进度、进行中的状态条、极少数需要用户立即注意的元素 |
| **Accent Glow** | `rgba(255, 188, 66, 0.2)` | 金色元素的弥散光晕，需极度克制 |
| **Success** | `#5A8F6E` | 任务完成、验证通过 |
| **Warning** | `#C47A5C` | 逾期、失败、警告（从 Sunflower Gold 衍生偏红） |
| **Text Primary** | `#192A51` | Display 大标题、核心数据数字 |
| **Text Body** | `#2D3A4F` | 正文、卡片标题、列表项标题 |
| **Text Secondary** | `#5A6A7E` | 描述、标签、占位符、辅助图标 |
| **Text Muted** | `rgba(90, 106, 126, 0.5)` | 时间戳、极次要信息 |
| **Border** | `#084B83` | 标准组件粗线边框（2px 实线） |
| **Border Light** | `rgba(8, 75, 131, 0.15)` | 次级分隔线、非聚焦状态的细线（1px） |
| **Border Accent** | `rgba(255, 188, 66, 0.5)` | 当前进度条边框、进行中的状态边界 |

### 2.3 色彩使用规则

**规则 1：背景层级（全屏背景图之上的层级）**
```
全屏背景图     → `绯夏主界面.png`（所有功能页面统一）
  页面背景     → Alice Blue #DCEDFF（全局天空兜底色）
    内容卡片   → White #FFFFFF（纯白浮岛，直角粗线边框）
      文本框   → White #FFFFFF 或 Alice Blue #DCEDFF（2px 实线边框包裹）
```
浅色主题通过**留白**与**边框**创造层级，而非阴影。直角边框本身即是强烈的层级语言。

**规则 2：冷暖对撞与克制（核心视觉策略）**
- 冷色（蓝系 + 白）占 90% → 建立清爽、理性、机能的氛围
- 暖色（向日葵金）占 8% → 仅用于"此时此刻正在进行"的核心状态，如进度条、当前选中
- 中性灰占 2% → 辅助信息
- **黄金色的使用原则**：像现实中的黄色警示带——细窄、精准、不可替代。严禁大面积金色背景或金色按钮。

**规则 3：文字对比**
- Display 标题在 Alice Blue 上：Space Indigo #192A51，对比度 12.4:1 ✓
- 正文在 White 上：#2D3A4F，对比度 10.8:1 ✓
- 次要文字使用 Slate Grey #5A6A7E
- **严禁使用纯黑 `#000000`**，也严禁使用纯蓝 `#0000FF` 等屏幕原生高饱和色

**规则 4：绯夏角色的色彩融合**
- 银白双麻花辫 → 对应 White 卡片与背景图的浅色调（天然融合）
- 蓝色小花与工牌挂绳 → 对应 Primary #084B83（主功能色呼应）
- 黑色机能服装 → 在浅色背景上形成强烈的机能剪影，无需额外描边
- 向日葵金 #FFBC42 作为"机能指示灯" → 仅在她的对话气泡尖角、当前任务指示器等极细处点缀

---

## 三、字体系统

### 3.1 字体栈

```css
font-family: 'Noto Sans SC', 'PingFang SC', 'Hiragino Sans GB', 'Microsoft YaHei', sans-serif;
```

**备用栈**：
- 英文/数字/数据：`Inter`, `SF Pro Display`
- 等宽数据（如倒计时、数值）：`JetBrains Mono`, `SF Mono`

### 3.2 字号规范

| Token | 字号 | 字重 | 行高 | 字间距 | 用途 |
|-------|------|------|------|--------|------|
| **Display** | 36px | 900 | 100% | -0.02em | 页面大标题（如启动界面 "LIFELONG-RPG"） |
| **H1** | 24px | 800 | 120% | -0.01em | 区块标题、主界面左上 "HINATSU" |
| **H2** | 16px | 700 | 140% | 0 | 卡片标题、弹窗标题、列表项主标题 |
| **Body** | 14px | 400 | 160% | 0 | 正文、描述 |
| **Caption** | 12px | 600 | 150% | 0.02em | 标签、按钮文字、导航项 |
| **Micro** | 10px | 700 | 140% | 0.05em | 时间戳、角标、MONO 数据、全大写英文 |

### 3.3 字体风格

**标题处理（标志性/核心文本）**：
- **页面级 Display 标题**：使用 **Display** + Space Indigo #192A51，**必须加粗至 900**
- **区块 H1 标题**：使用 **H1** + Space Indigo #192A51，**必须加粗至 800**
- 标题下方可带 4px 高的 Yale Blue 粗线装饰，宽度为文字长度的 40%，0px 圆角
- 英文副标题使用 **Micro** + 全大写 + Slate Grey #5A6A7E，字间距 0.05em

**数据/状态标签**：
- 所有状态、稀有度、数值标签使用 **Micro** + 全大写 + 等宽字体，**必须加粗至 700**
- 例：`[PROGRESS]`、`ACTIVE`、`+1250`

**无背景指引组件的文字**：
- 导航项、操作指引使用 **Caption** + 字重 600，颜色 #2D3A4F 或 #5A6A7E
- 当前选中态：颜色 #084B83，**下方带 2px 实线指示条**

---

## 四、间距与布局

### 4.1 间距体系（4px 基座）

```
4px   → 图标内边距、紧凑行间距
8px   → 小间隙、标签内部 padding
12px  → 卡片内部小间距
16px  → 标准卡片 padding、列表项间距
20px  → 区块间距
24px  → 大区块分隔
32px  → 页面顶部安全区
```

### 4.2 布局网格

- **移动端基准**：375px 视口，内容区 343px（左右各 16px margin）
- **主界面布局**：左侧 55% 为功能与文本区，右侧 45% 为绯夏立绘区，中间严格分界
- **卡片网格**：2 列等宽，gap 12px
- **信息层级**：Z-index 分层
  - 底层：`绯夏主界面.png` 全屏背景图
  - 页面层：背景兜底色 `#DCEDFF`
  - 内容层：卡片、文字、无背景指引组件
  - 悬浮层：Toast、下拉菜单
  - 模态层：弹窗遮罩 `rgba(25, 42, 81, 0.35)` + backdrop-blur
  - 顶层：全局加载、紧急通知

### 4.3 安全区

```
顶部：32px（状态栏避让）
底部：80px（底部操作区高度 64px + 16px 安全距离）
左右：16px
```

---

## 五、组件规范

### 5.1 按钮（Button）

**主按钮（Primary）**
```
背景：   #084B83
文字：   #FFFFFF
字号：   Caption (12px)
字重：   700
Padding：12px 28px
边框：   无
圆角：   0px（直角）
阴影：   无（拒绝弥散阴影，用颜色对比建立层级）
悬停：   背景 → #0A5CA0
点击：   scale(0.97)，背景 → #063A66
```

**次按钮（Secondary）**
```
背景：   transparent
边框：   2px solid #084B83
文字：   #084B83
Padding：10px 26px
圆角：   0px
悬停：   背景 → rgba(8, 75, 131, 0.06)
```

**幽灵按钮（Ghost）**
```
背景：   transparent
边框：   无
文字：   #5A6A7E
悬停：   文字 → #084B83
使用场景：无背景指引区的操作项
```

**图标按钮（Icon Button）—— 机能风核心**
```
背景：   transparent
边框：   2px solid #084B83
尺寸：   48px × 48px
圆角：   0px
图标：   24px，颜色 #084B83
悬停：   背景 → #084B83，图标 → #FFFFFF
点击：   scale(0.95)
使用场景：主界面左下角的功能方块（如实心/空心斜线方块）
```

### 5.2 卡片（Card）

**标准内容卡片**
```
背景：   #FFFFFF
边框：   2px solid #084B83（粗线边框是核心特征）
圆角：   0px（绝对直角，传递机能感）
Padding：16px
阴影：   无（拒绝弥散阴影）
悬停：   背景 → #F0F7FF，边框 → 2px solid #0A5CA0
```

**选中卡片**
```
背景：   #FFFFFF
边框：   2px solid #084B83
内部左侧：4px 实色指示条 #084B83
```

**任务节点卡片（特殊）**
```
背景：   #FFFFFF
边框：   2px solid 对应颜色
左侧：   4px 实色指示条
Padding：12px
圆角：   0px
```

任务类型色：
| 类型 | 背景 | 边框/指示条 |
|------|------|------------|
| 纪元 (Goal) | `#FFFFFF` | `2px solid #084B83` |
| 主线 (Main) | `#FFFFFF` | `2px solid #94B0DA` |
| 支线 (Sub) | `#FFFFFF` | `2px solid #5A6A7E` |
| 今日 (Daily) | `#FFFFFF` | `2px solid #5A8F6E` |
| 进行中 | `#FFFFFF` | `2px solid #FFBC42`，左侧指示条 `#FFBC42` |

### 5.3 文本框（Input）—— 直角粗线核心组件

```
背景：   #FFFFFF
边框：   2px solid #084B83（粗线、直角）
圆角：   0px
Padding：12px 16px
文字：   #2D3A4F
占位符： #5A6A7E
聚焦：   边框 → 2px solid #0A5CA0，背景 → #F0F7FF
错误：   边框 → 2px solid #C47A5C
```

### 5.4 标签（Tag / Badge）

**稀有度标签**
```
背景：   transparent（无背景色块）
边框：   2px solid
Padding：2px 8px
字号：   Micro (10px)
字重：   700
全大写
圆角：   0px
```

| 稀有度 | 边框/文字色 |
|--------|------------|
| N | `#5A6A7E` |
| R | `#94B0DA` |
| SR | `#084B83` |
| SSR | `#FFBC42`（金色边框，仅此处可用金色） |

**状态标签**
```
已完成：  无背景，文字 #5A8F6E，左侧 4px 指示条 #5A8F6E
进行中：  无背景，文字 #FFBC42，左侧 4px 指示条 #FFBC42
已逾期：  无背景，文字 #C47A5C，左侧 4px 指示条 #C47A5C
```

### 5.5 弹窗 / 模态框（Modal）

**通用弹窗容器**
```
遮罩：   rgba(25, 42, 81, 0.35) + backdrop-blur: 16px
容器：   背景 #FFFFFF，边框 2px solid #084B83
圆角：   0px
Padding：28px
阴影：   无
动画：   从底部滑入 + 淡入，250ms，cubic-bezier(0.16, 1, 0.3, 1)
```

**登录 / 注册弹窗（启动界面专用）**
```
触发方式：
  - 首次打开应用自动弹出
  - 或在启动界面点击任意非交互区域后弹出
  - 或在启动界面下滑/上滑呼出

布局：
  遮罩：   rgba(25, 42, 81, 0.45) + backdrop-blur: 20px（比通用弹窗更深，突出沉浸感）
  容器：   背景 #FFFFFF，边框 2px solid #084B83，0px 圆角
  宽度：   320px（移动端），居中显示
  Padding：32px

标题区：
  标题：   H1 24px + Space Indigo #192A51 + 字重 800
          "登录" / "注册" / "WELCOME BACK"
  装饰：   标题下方 4px 宽 Yale Blue 粗线，宽度 40px

输入区：
  账号/邮箱：文本框（2px 粗线直角 #084B83 边框）
  密码：     同上，带可见性切换图标
  验证码：   横向排列，输入框 + Primary 按钮发送

操作区：
  主按钮：   Primary 样式，文字 "登录" / "注册"
  切换链接： Ghost 样式，"还没有账号？立即注册"

第三方登录（可选）：
  分隔线：   1px solid rgba(8, 75, 131, 0.15)
  图标按钮： Icon Button 样式，48px 直角边框方块排列
```

### 5.6 底部导航 / 操作区（Tab Bar）

**核心原则：无背景色块，纯图标+加粗文字**

```
背景：   transparent（拒绝白色卡片背景，露出底层背景图）
上边框： 2px solid rgba(8, 75, 131, 0.1)
高度：   64px
图标：   24px
文字：   Caption (12px)，字重 600

未选中： 图标 #5A6A7E，文字 #5A6A7E
选中：   图标 #084B83，文字 #084B83，**文字下方 2px 实线指示条 #084B83**
安全区： 底部额外 16px padding-bottom
```

### 5.7 进度条 / 指示器（Progress）

```
背景轨道：2px solid rgba(8, 75, 131, 0.1)，高度 4px
当前进度：#FFBC42（向日葵金），高度 4px
已完成段：#5A8F6E
```

### 5.8 分隔线（Divider）

```
标准：   2px solid #084B83（粗线，用于区块分割）
次级：   1px solid rgba(8, 75, 131, 0.15)（细线，用于列表项分割）
```

---

## 六、图标系统

### 6.1 风格

- **线框图标（Outline）**：2px 线宽，直角端点（与组件直角语言一致）
- **填充图标（Solid）**：仅用于选中态和极少数强调
- **尺寸**：标准 24px，小号 20px，大号 32px
- **机能风特征**：图标可带"断裂"、"斜切"、"像素化"等机能细节

### 6.2 颜色规则

```
默认：     #5A6A7E
悬停/激活： #084B83
进行中/强调：#FFBC42（极度克制）
成功：     #5A8F6E
错误：     #C47A5C
```

### 6.3 特殊图标处理

**底部导航图标**：无背景色块，纯图标+文字
- 主页：`◈`（竖菱形，机能感）
- 任务：`⚔`（交叉剑）
- 商店：`♦`（菱形）
- 社区：`◎`（靶心）
- 好友：`☆`（空心星）
- 我的：`▣`（方框靶心）

**主界面左下角功能方块**：
- 实心方块 + 白色斜线 = 已激活/当前视图
- 空心边框 + 蓝色斜线 = 可切换视图

---

## 七、动画与交互

### 7.1 动画原则

**干脆、精准、低弥散**

| 场景 | 时长 | 缓动 | 效果 |
|------|------|------|------|
| 页面切换 | 200ms | ease-out | 淡入 + 轻微上移 4px |
| 卡片悬停 | 120ms | ease | 背景色变化，无位移（拒绝上浮阴影） |
| 按钮按下 | 60ms | ease-in | scale(0.97) |
| 弹窗出现 | 250ms | cubic-bezier(0.16, 1, 0.3, 1) | 从底部滑入 16px + 淡入 |
| 弹窗消失 | 150ms | ease-in | 向下滑出 8px + 淡出 |
| 数值变化 | 500ms | ease-out | 数字滚动计数 |
| 任务完成 | 600ms | spring | 卡片边框闪烁一次 #5A8F6E + 左侧指示条延展 |
| 角色台词 | 300ms | ease | 淡入，无打字机（保持干练） |

### 7.2 角色相关动画

**绯夏立绘呼吸**：
```
animation: float 4s ease-in-out infinite;
transform: translateY(±4px)
```
幅度极小，拒绝过度活泼，保持机能感。

**台词气泡出现**：
```
初始：opacity: 0, translateY(4px)
结束：opacity: 1, translateY(0)
时长：200ms
边框：2px solid #084B83，0px 圆角
```

**好感度进度条**：
```
时长：600ms
缓动：cubic-bezier(0.34, 1.56, 0.64, 1)（轻微过冲）
颜色：#FFBC42（向日葵金），拒绝渐变，纯色干练
```

### 7.3 微交互

**按钮悬停（机能风）**：
- 边框颜色加深
- 无左侧箭头指示器（拒绝 P5 式箭头，保持纯粹几何）
- 无位移（拒绝左移效果）

**任务勾选**：
- 勾选瞬间：对勾从中心 scale(0) → scale(1)
- 未完成任务左侧小蓝点：脉冲动画，2px 实心圆

**当前状态指示**：
- 进行中的任务节点：左侧 4px 金色指示条 #FFBC42 轻微脉动（opacity 0.6 ↔ 1.0，1.5s 周期）

---

## 八、Imagery 与角色展示

### 8.1 全局背景规范

**所有功能页面统一背景**：
```
背景图：   `绯夏主界面.png`
填充方式： object-fit: cover，居中显示
层级：     固定于 z-index: -1，不可滚动

背景图之上：
  - 全局覆盖一层极淡的 Alice Blue #DCEDFF（opacity 0.15-0.25）
  - 确保文字可读性，同时保留背景图的渐变与几何纹理
  - 内容卡片使用 White #FFFFFF，与背景图形成天然层级
```

**背景图元素利用**：
- 左上角的 "LIFELONG-RPG HINATSU" 水印式标题 → 可作为页面标题区的视觉参考，实际 UI 标题叠加于其上或下方
- 右侧大型圆弧 → 可作为绯夏立绘区的背景装饰，不额外添加装饰元素
- 左下角点阵矩阵 → 可作为底部导航区的背景纹理

### 8.2 绯夏立绘展示规范

**主界面展示**：
```
尺寸：   占屏幕右侧 40-45%，高度自适应，底部可裁切
位置：   固定于右侧，垂直居中偏下
动画：   持续 gentle float（±4px，4s周期，极度克制）
阴影：   无（拒绝角色阴影，保持画面干净）
裁切：   允许下半身超出屏幕底部（半身像焦点在面部与上半身机能细节）
```

**角色融合要点**：
- 绯夏的黑色机能服装在背景图的浅蓝渐变上形成强烈、干净的剪影
- 她发饰的蓝色小花与 Yale Blue #084B83 形成色彩呼应
- 银白头发与白色卡片背景形成微妙层次
- 向日葵金 #FFBC42 **绝不直接出现在角色身上**，仅作为 UI 状态指示色

### 8.3 商品/奖励图标

- 未解锁：Slate Grey 锁形占位 + 低透明度
- 已拥有：左侧 4px #5A8F6E 指示条
- 可购买：左侧 4px #084B83 指示条，价格文字 #2D3A4F

---

## 九、页面级示例

### 9.1 启动界面（Splash / Awakening）

```

### 9.2 登录 / 注册弹窗（启动界面 Overlay）



关闭：   点击遮罩区域关闭弹窗，返回启动界面
```

### 9.3 主界面（Home）

```
背景：           `绯夏主界面.png` 全屏覆盖（所有拓展界面共享此背景）
全局覆盖：       Alice Blue #DCEDFF 半透明层（opacity 0.15），确保文字可读

左上角标题区：
  英文：   Caption 12px + Slate Grey #5A6A7E，"LIFELONG-RPG"
  主标题： H1 24px + Space Indigo #192A51 + 字重 800，"HINATSU"
  装饰：   标题下方 4px Yale Blue 粗线

左侧功能区（55% 宽度）：
  今日任务列表：无背景卡片，Caption 600 #2D3A4F
  当前选中：    文字 #084B83 + 下方 2px 实线指示条
  状态数据：    Micro 700 等宽字体，Space Indigo #192A51

右侧角色区（45% 宽度）：
  绯夏立绘：固定右侧，占高 80%，底部裁切
  无额外装饰：背景图的圆弧自然作为角色背景
```

### 9.4 拓展界面（Quest / Shop / Community / Friends / Profile）

```
背景：           与主界面完全一致，`绯夏主界面.png` 全屏覆盖
                 所有拓展界面共享同一背景，保持视觉统一

布局规则：
  - 顶部保留 32px 安全区
  - 页面标题：H1 24px + Space Indigo #192A51 + 字重 800
  - 内容区：White #FFFFFF 直角粗线卡片，在背景图上形成"浮岛"
  - 拒绝为不同页面更换背景色或背景图

视觉一致性：
  - 背景图的几何圆弧、点阵、网格在所有页面保持静止
  - 页面切换时背景图不动，仅内容层做 200ms 淡入动画
  - 确保用户始终感受到统一的"晴空"氛围
```

### 9.5 任务详情弹窗（Modal）

```
遮罩：           Overlay rgba(25, 42, 81, 0.35) + backdrop-blur: 16px
                 背景图在遮罩后依然可见，保持沉浸感
弹窗背景：        White #FFFFFF，边框 2px solid #084B83，0px 圆角
标题栏：         H2 16px + Space Indigo #192A51 + 字重 700
                 下方 2px Yale Blue 分隔线
内容文字：        Body 14px #2D3A4F
验收标准：        Caption 12px #084B83，左侧 4px 蓝色指示条
截止日期：        Caption 12px #5A6A7E
按钮：           Primary #084B83 + Ghost 取消
```

---

## 十、禁忌清单（Don'ts）

| ❌ 禁止 | ✅ 替代方案 |
|--------|-----------|
| 启动界面上放置固定按钮破坏画面 | 登录注册以弹窗 Overlay 呼出，保持全屏画面完整 |
| 不同页面使用不同背景图 | 所有功能页面统一使用 `绯夏主界面.png` 作为背景 |
| 使用圆角边框（> 0px） | 所有组件使用 0px 圆角，直角传递机能感 |
| 使用细线边框（< 2px）作为主要边界 | 核心组件边框最小 2px 实线 |
| 指引性组件使用背景色块包裹 | 无背景，纯图标+加粗文字，用指示条/下划线标识选中态 |
| 使用弥散阴影创造层级 | 用留白、2px 边框、背景色差异创造层级 |
| 过度使用 Sunflower Gold | 金色仅用于当前进度与极少数状态指示，控制在 5% 以内 |
| 使用纯黑 `#000000` 作为文字色 | 使用 Space Indigo #192A51 或 #2D3A4F |
| 角色立绘使用发光/描边效果 | 依靠角色本身在浅色背景上的天然剪影对比 |
| 使用渐变色作为主按钮背景 | 使用纯色 Yale Blue + 0px 圆角 |
| 在浅色背景上使用红色作为错误色 | 使用 #C47A5C（偏暖棕红），更和谐 |
| 忽视直角语言的一致性 | 从按钮到卡片到弹窗，全部保持 0px 圆角 |

---

## 十一、Token 汇总表（开发直接用）

```css
:root {
  /* 背景 */
  --bg-primary: #DCEDFF;
  --bg-surface: #FFFFFF;
  --bg-surface-hover: #F0F7FF;
  --bg-surface-elevated: #084B83;
  --bg-overlay: rgba(25, 42, 81, 0.35);
  --bg-overlay-deep: rgba(25, 42, 81, 0.45);

  /* 强调 */
  --color-primary: #084B83;
  --color-primary-hover: #0A5CA0;
  --color-primary-active: #063A66;
  --color-accent: #FFBC42;
  --color-accent-glow: rgba(255, 188, 66, 0.2);
  --color-success: #5A8F6E;
  --color-warning: #C47A5C;

  /* 文字 */
  --text-display: #192A51;
  --text-primary: #2D3A4F;
  --text-secondary: #5A6A7E;
  --text-muted: rgba(90, 106, 126, 0.5);

  /* 边框 */
  --border-default: 2px solid #084B83;
  --border-light: 1px solid rgba(8, 75, 131, 0.15);
  --border-accent: 2px solid rgba(255, 188, 66, 0.5);

  /* 字体 */
  --font-body: 'Noto Sans SC', 'PingFang SC', sans-serif;
  --font-mono: 'JetBrains Mono', 'SF Mono', monospace;

  /* 间距 */
  --space-xs: 4px;
  --space-sm: 8px;
  --space-md: 12px;
  --space-lg: 16px;
  --space-xl: 20px;
  --space-2xl: 24px;
  --space-3xl: 32px;

  /* 圆角（机能风：全部直角） */
  --radius-none: 0px;

  /* 阴影（拒绝弥散阴影） */
  --shadow-none: none;
}
```

---

*Questopia Design System v3.0 · 机能晴空主题 · 基于绯夏机能风造型 · 融合绝区零 × 明日方舟工业美学*
